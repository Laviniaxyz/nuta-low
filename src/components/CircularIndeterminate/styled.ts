import styled from 'styled-components';
import CircularProgress from '@mui/material/CircularProgress';

export const Components = {
  Container: styled(CircularProgress)`
    display: flex;
    align-items: center;
    justify-content: space-between;
  `,
};
